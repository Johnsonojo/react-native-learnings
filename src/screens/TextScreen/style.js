import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    borderColor: "black",
    borderWidth: 1,
    height: 40,
    borderRadius: 5,
  },
  warning: { color: "red" },
});

export default styles;
